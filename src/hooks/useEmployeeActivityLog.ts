// Employee activity logging has been removed. This stub prevents import errors.
export function useEmployeeActivityLog() {
  return {
    logActivity: async (_action: string, _details?: string) => {},
  };
}
